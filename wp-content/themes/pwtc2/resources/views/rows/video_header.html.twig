<section class="video text-center">
    <div>
        <div class="content">
            <h1>{{ row.title }}</h1>
            <a href="{{ row.video }}" class="fancybox-media"><i class="fa fa-play"></i></a>
        </div>
        <img src="{{ row.mobile_background_image.url }}" class="hide-for-medium" alt="">
        <img src="{{ row.desktop_background_image.url }}" class="hide-for-small-only" alt="">
    </div>
</section>
